<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="@dimen/padding_standart_half"
        app:cardElevation="@dimen/padding_standart_half"
        android:layout_margin="@dimen/padding_standart_half">

    <android.support.constraint.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <ImageView
                android:id="@+id/player_image"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:padding="@dimen/padding_standart_half"
                android:src="@mipmap/ic_launcher_round"
                app:layout_constraintEnd_toStartOf="@id/player_name"
                android:contentDescription="photo"/>

        <TextView
                android:id="@+id/player_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="@string/new_player"
                android:textSize="@dimen/text_size_name"
                android:maxLines="1"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintStart_toEndOf="@id/player_image"
                android:layout_marginStart="@dimen/padding_standart_half"
                android:layout_marginEnd="@dimen/padding_standart_half"/>

    </android.support.constraint.ConstraintLayout>

</android.support.v7.widget.CardView>